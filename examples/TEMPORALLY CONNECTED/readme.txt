tc_lvl_1:

description : find the temporal path. 

hint : The tree must shine!

three_paths:

description : Find a way to compute the TC beetween A and D using the 3 possibles paths.

hint : - Strict/non-strict
       - For the third, try to change the label of one node.

question : try to compute the TC of just one node, can you interpret the result ?

graph_tc_strict:

description : Is the graph tc ? and for the strict case ?

hint : ...

graph_tc_non_strict:

description : Is the graph tc ? and for the strict case ?

hint : ...

