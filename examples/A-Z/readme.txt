empty:

description : Create an graph from scratch, using the differents features availables.

node A : shortcut
node B : drag
node C : button
edge : button no label, button label, drag + label
zoom : +, -, reset
edition mode : change node/edge name/label
arrow : left/right/up/down + reset

to_delete:

description : Delete a graph, using the diferents features.

delete a node : button + shortcut
delete an edge : button + shortcut



